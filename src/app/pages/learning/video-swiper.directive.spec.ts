import { VideoSwiperDirective } from './video-swiper.directive';

describe('VideoSwiperDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoSwiperDirective();
    expect(directive).toBeTruthy();
  });
});
